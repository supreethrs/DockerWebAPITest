apiVersion: v1
kind: Service
metadata:
  labels:
    app: webapitest
    component: frontend
  name: frontend
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: webapitest
    component: frontend
  type: LoadBalancer